Логические операции
И(одновременное выполнение условий)
a > 0 && a < 25

ИЛИ(выполнение хотя-бы одного из условий)
a == 0 || b == 0

НЕ
!(a >= 0)


continue; сразу  переходим к следующей операции

numbers.push(randomNumber); Пушит значение из переменной(randomNumber) в переменную(numbers)
words.splice(i, 1); Удаляет строку из переменной(words), строка определяется в скобках 

function zametka(n){
....
}; Позволяет выполнять определенные действия n-е кол-во раз и выолнять их в определенных местах, избегая копипаста.
Math.round - округление
return - возвращает результат туда, откуда был сделан запрос